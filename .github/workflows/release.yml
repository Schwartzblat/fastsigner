name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: cargo build --release --locked

      - name: Package
        run: tar czf fastsigner-x86_64-unknown-linux-gnu.tar.gz -C target/release fastsigner

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" fastsigner-x86_64-unknown-linux-gnu.tar.gz --title "${{ github.ref_name }}" --generate-notes --repo "${{ github.repository }}"
